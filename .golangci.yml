version: "2"

# Force cache invalidation for build tag fix
linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
  exclusions:
    generated: lax
    rules:
      - linters:
          - errcheck
        path: _test\.go
      - path: ".*\\.gen\\.go"
        linters:
          - govet
          - errcheck
          - staticcheck
          - ineffassign
      - path: "contracts/ethereum/bindings/go/"
        text: "local import"
